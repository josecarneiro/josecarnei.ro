{
  "name": "@josecarneiro/root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "deploy:clean": "rm -rf dist",
    "deploy:app": "cp -R packages/app/out dist",
    "deploy:resume": "cp packages/resume/dist/jose-carneiro-curriculum-vitae.pdf dist/curriculum-vitae.pdf",
    "deploy": "npm run deploy:clean && npm run deploy:app"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
