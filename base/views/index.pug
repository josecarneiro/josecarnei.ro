extends layout/default

block content
  .container
    .vertical-align-container
      .vertical-align
        h1 José Maria Carneiro
        h2 Business Student, Web Developer
        ul
          each link in data.social
            li
              a(href= link.url) #{link.name}
